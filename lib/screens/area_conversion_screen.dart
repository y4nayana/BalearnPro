import 'package:flutter/material.dart';

class AreaConversionScreen extends StatelessWidget {
  const AreaConversionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Konversi Area"),
      ),
      body: const Center(
        child: Text("Halaman Konversi Area"),
      ),
    );
  }
}
